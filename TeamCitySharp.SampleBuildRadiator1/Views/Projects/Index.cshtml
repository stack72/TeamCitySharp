@model List<TeamCitySharp.DomainEntities.Project>

@{
    ViewBag.Title = "Index";
}

<h2>Project List</h2>
<ul>
    @foreach (var project in Model)
    {
        <li>@project.Name</li>
    }
</ul>
